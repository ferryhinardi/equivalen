version: 2

defaults: &defaults
  docker:
    - image: circleci/node:8.2.1
  working_directory: ~/workspace

jobs:
  clone_and_hash:
    <<: *defaults
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-project-metadata-{{ .Branch }}
            - v1-project-metadata-master

      - run:
          name: Initialize project metadata
          command: |
            mkdir -p .project-metadata
            touch .project-{www,landing}-hash

      - run:
          name: Hash projects
          command: |
            git log --pretty=format:'%H' -n 1 -- www > .project-www-hash.new
            git log --pretty=format:'%H' -n 1 -- landing > .project-landing-hash.new

      - run:
          name: Determine changed projects
          command: |
            if ! diff .project-www-hash{,.new} > /dev/null; then
              touch .project-metadata/.project-www-changed
            fi

            if ! diff .project-landing-hash{,.new} > /dev/null; then
              touch .project-metadata/.project-landing-changed
            fi

      - run:
          name: Set current project hashes
          command: |
            mv .project-www-hash{.new,}
            mv .project-landing-hash{.new,}

      - persist_to_workspace:
          root: .
          paths:
            - .project-metadata

      - save_cache:
          key: v1-project-metadata-{{ .Branch }}-{{ epoch }}
          paths:
            - .project-www-hash
            - .project-landing-hash

  www:
    docker:
      - image: electronuserland/electron-builder:wine
    working_directory: ~/workspace
    steps:
      - checkout: { path: ~/workspace }
      - attach_workspace: { at: . }
      - run:
          name: Check whether project changed
          command: |
            if [ -e .project-metadata/.project-www-changed ]; then
              echo "let's do this"
            else
              echo "nothing to do"
            fi

      - run:
          name: Install lerna
          command: yarn

      - run:
          name: Install package
          command: node_modules/lerna/bin/lerna.js bootstrap

      - run:
          name: Build package rp-electron-storage
          command: node_modules/lerna/bin/lerna.js run build --scope equivalen-redux-persist-electron-storage

      - run:
          name: Build package simple-radio-button
          command: node_modules/lerna/bin/lerna.js run build --scope equivalen-simple-radio-button

      - run:
          name: Build
          command: CI=false node_modules/lerna/bin/lerna.js run build --scope exmedia

      - run:
          name: Test
          command: node_modules/lerna/bin/lerna.js run test:flow --scope exmedia

      - run:
          name: Publish Github
          command: GH_TOKEN=3c908b7912734ab6949dc474d43c40a6bf313de4 node_modules/lerna/bin/lerna.js run pack --scope exmedia
          no_output_timeout: 15m

  landing:
    docker:
      - image: circleci/node:8.11.2-jessie
    working_directory: ~/workspace
    steps:
      - checkout: { path: ~/workspace }
      - attach_workspace: { at: . }
      - run:
          name: Check whether project changed
          command: |
            if [ -e .project-metadata/.project-landing-hash ]; then
              echo "let's do this"
            else
              echo "nothing to do"
            fi
      - run:
          name: Deploy Heroku
          command: cd source/landing && git push https://heroku:$HEROKU_API_KEY@git.heroku.com/$HEROKU_APP_NAME.git master

workflows:
  version: 2

  build_test_deploy:
    jobs:
      - clone_and_hash
      - www:
          requires:
            - clone_and_hash
          filters:
            branches:
              only: master
      - landing:
          requires:
            - clone_and_hash
          filters:
            branches:
              only: master